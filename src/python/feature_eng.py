import pandas as pd

#function to engineer data columns into features for training
def engineer_features (df: pd.DataFrame) -> pd.DataFrame:
    df = df.copy()

    


#columns used as features in training
feature_cols - [
    """

    """
]

